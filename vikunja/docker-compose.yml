services:
  vikunja:
    image: vikunja/vikunja
    container_name: vikunja
    restart: unless-stopped
    env_file:
      - ./.env
    volumes:
      - ./files:/app/vikunja/files
      - ./db:/db
    networks:
      - proxy

networks:
  proxy:
    external: true
